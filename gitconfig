[user]
name = Rafael Alencar
email = rafael.alencar@movile.com

[alias]
ap = add -p
cm = commit -m
co = checkout
dc = diff --cached
di = diff
pr = pull --rebase
st = status
branches = for-each-ref --sort=-committerdate --format=\"%(color:blue)%(authordate:relative)\t%(color:red)%(authorname)\t%(color:white)%(color:bold)%(refname:short)\" refs/remotes

[push]
	default = simple

[core]
	excludesfile = /Users/rafael.alencar/.gitignore_global
	editor = vim

[merge "mergepbx"]
        name = XCode project files merger
        driver = mergepbx %O %A %B
